export default async function preview({ flags, args, packageRoot }) {
    const port = flags.port || flags.p || 3000;
    const host = flags.host || 'localhost';

    console.log(`INIT`);

    // Your preview logic here
}